{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".", // Base URL for resolving modules
    "paths": {
      "@env": ["env.d.ts"] // Map @env to your custom declaration
    }
  }
}
